/*
 * var, let and const
 * var is not used anymore
 * don't start variable names with numbers, * or special characters like "let 99name"
 * */

//let value could  be re-assigned
let client = "Victor"
client = 30
console.log(client)

//let variables could be initialized without default value
let price
price = 1000
console.log(price)
